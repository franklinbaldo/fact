name: FACT Conformance

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  complete-dogfooding:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Require complete native FACT dogfooding
        run: python scripts/check_dogfooding.py .
      - name: Require language-neutral ignore conformance
        run: python scripts/check_ignore_conformance.py
      - name: Require language-neutral local type resolution
        run: python scripts/check_local_type_resolution.py
      - name: Require relational capability boundary
        run: python scripts/check_relational_capability_boundary.py
      - name: Require composed context identity conformance
        run: python scripts/check_composed_context_identity.py
