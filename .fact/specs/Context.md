---
id: type:Context
type: .fact/specs/TypeSpecification.md
title: Context
---

# Context

A `Context` fact anchors a native FACT context and carries the context's stable
identity in its authored `id`.

A context is discovered by `.fact/`; exactly one direct Markdown fact in that
directory has type `.fact/specs/Context.md`. Its filename is conventional, not
semantic.
