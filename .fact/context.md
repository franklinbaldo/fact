---
id: urn:fact:context:fact
type: .fact/specs/Context.md
title: FACT
---

# FACT context configuration

This control document configures the repository's native FACT context. Its authored
`id` is the stable context identity.

Because this file lives under `.fact/`, it is **not** part of the context's fact
set. `.fact/` is the context control plane; Markdown is preferred here for authored
configuration, but control documents remain distinct from semantic facts.
