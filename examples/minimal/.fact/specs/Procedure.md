---
kind: type-spec
name: Procedure
---

# Procedure

A `Procedure` fact records a repeatable procedure. The example uses `title`,
`description`, and producer-defined fields such as `owner`.

This specification is control-plane Markdown under `.fact/`; it constrains facts
without itself becoming a fact.
